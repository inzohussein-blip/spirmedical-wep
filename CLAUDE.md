# سباير ميديكال — ملاحظاتٌ للجلسات القادمة

منصّة طبّية عربية (RTL) — Next.js 14 App Router + Supabase.

## 🗺️ خريطة سريعة — أين تجد ماذا

الخريطةُ المفصّلة (المسارات كلُّها، الجداول، الكرون، المتغيّرات) في
[`README.md`](README.md). هذا مختصرُها لبدء أيّ مهمّة. المسارات نسبيّةٌ إلى
`src/` ما لم يُذكر غيرُ ذلك.

| أبحث عن… | اذهب إلى |
|---|---|
| توجيه النطاق والجلسة | `middleware.ts` · `lib/site-config.ts` · `lib/supabase/middleware.ts` |
| الجلسة والأدوار | `lib/auth/session.ts` (`requireSession`) · تخطيطُ كلّ مجموعة `app/(…)/layout.tsx` |
| عملاء Supabase | `lib/supabase/server.ts` (RLS) · `server-service.ts` (service_role) · `client.ts` |
| رفع الطلب (المريض) | `app/(dashboard)/appointments/new/` → `NewAppointmentClient.tsx` + `actions.ts` |
| تدفّقات الطلب | `components/appointments/BloodDrawFlow.tsx` · `NursingFlow.tsx` · `AppointmentWizard.tsx` |
| التحقّق من النماذج | `lib/validations/*` · `lib/forms/useFormErrors.ts` · `components/forms/MissingFieldsSummary.tsx` |
| طلبات المختصّ وأفعاله | `app/(specialist)/specialist/orders/[id]/actions.ts` · `role-forms/*` |
| إعداد الخدمات والشبكة | `lib/services-v3.ts` (`bookingSoon`، الشارات) · `lib/service-switches.ts` |
| بيانات الخدمات والتحاليل | `lib/services/services-data.ts` · `blood-tests-data.ts` · `labs-data.ts` · `time-slots.ts` |
| التغطية الجغرافية | `lib/service-areas.ts` · `lib/hooks/useServiceCoverage.ts` · الفهرسة `lib/seo/coverage.ts` |
| الإشعارات | `lib/notifications.ts` (الطابور) · `lib/notifications-processor.ts` · `lib/services/push*.ts` · `lib/services/whatsapp.ts` |
| رموز الدخول | `app/(auth)/login/actions.ts` · `lib/whatsapp/otp-service.ts` · `lib/auth/otp-mode.ts` |
| الغلاف والتنقّل | `components/layout/AppShell.tsx` · `AuthenticatedShell.tsx` · `lib/focused-routes.ts` |
| النوافذ المنبثقة | `components/ui/ModalShell.tsx` · `BottomSheet.tsx` · `lib/hooks/useModalDialog.ts` |
| هواتف المنشآت | `components/ui/PhoneLink.tsx` · `lib/format/phone.ts` |
| التنسيق | `app/styles/shared.css` (رموز + حارس iOS) · `app.css` (شاشات التطبيق) · `marketing.css` · `admin.css` |
| الفهرسة | `app/layout.tsx` · `components/seo/JsonLd.tsx` · `app/sitemap.ts` · `app/robots.ts` |
| الكرون | `vercel.json` → `app/api/cron/*` · `app/api/notifications/process` |
| قاعدة البيانات | `supabase/migrations/` (آخرها 0047) · الأنواع `types/database.ts` (أعِد توليدها بعد كلّ ترحيل: `npm run db:types`) |
| الاختبارات | `tests/` — اسمُ الحارس يدلّ على مجاله (`rls-*`، `order-flow-modals`، `specialist-screens`…) |

**أدوات التحقّق البصريّ** (خارج المستودع، أُعيد بناؤها كلَّ جلسة): خادمٌ
يحاكي Supabase محلّياً + Playwright بـ`executablePath: '/opt/pw-browsers/chromium'`
بعرض 360×640 — لا تُنشئ حساباً تجريبياً في الإنتاج بلا إذن المالك.

## 📌 عملٌ مطلوبٌ لم يُنفَّذ بعد

لا شيء معلَّقٌ الآن. (الرفض التلقائيّ للطلبات المعلَّقة نُفِّذ في الترحيل
0042 — انظر أدناه.)

## ⚙️ ما يجب معرفته قبل التعديل

- **الفرع**: التطوير على `claude/project-analysis-a3arpj`. لا يُدفع إلى غيره
  بلا إذنٍ صريح، ولا يُفتح PR إلّا بطلب.
- **الإنتاج يُبنى من `main`** (Vercel، مشروع `spirmedical-wep`). الدمجُ فيه نشرٌ
  حيّ، فلا يكون إلّا بإذن المالك (دُمج بإذنه في 8719bd1).
- **الترحيلُ حيٌّ فوراً والكودُ لا يصل إلّا بالدمج.** ترحيلٌ يفترض الكودَ الجديد
  يكسر الموقعَ الحيّ في الفجوة: 0046 أغلق إدراج `bug_reports` فصار زرُّ الإبلاغ
  — الباقي في `main` — يُظهر خطأ RLS حتى دُمج الفرع. قبل أيّ ترحيلٍ اسأل: هل
  يعمل كودُ `main` الحاليّ معه؟ وإلّا فادمج أوّلاً أو أجّل الترحيل.
- **الترحيلات 0001–0010 ليست في سجلّ Supabase.** الجداول موجودةٌ لكنّها
  أُنشئت بطريقةٍ أخرى، فعباراتُ الإدراج فيها لم تُنفَّذ قطّ (انظر ترويسة
  0026). فأيّ إصلاحٍ يجب أن يكون ترحيلاً جديداً، لا تعديلاً لملفٍّ قديم
  يُتوقَّع إعادةُ تشغيله.
- **PostGIS مُسقَط عمداً** (0025). لا تُعده لميزةٍ واحدة: مناطق الخدمة
  تستعمل `jsonb` واختبارَ الشعاع في SQL خالص (0030).
- **`private` مخطَّطٌ للدوالّ المُفوَّضة** (0024) — لا تُنشئ `SECURITY DEFINER`
  جديدةً في `public` بلا سبب. وكلُّ دالّةٍ جديدة تُعلن
  `SET search_path = public, pg_temp` في تعريفها: سويبُ 0014 يعمل مرّةً
  عند النشر، وقد قُيّد بـ`public` وحدها حتى 0043 فلم يبلغ `private`.
- **`service_role` يتخطّى RLS ولا يتخطّى `GRANT`.** البوّابتان مستقلّتان.
  بقي الدورُ بلا منحةٍ حتى 0041، فكان كلُّ ما يمرّ بـ`createAdminClient()`
  يُردّ 42501 — حتى قراءةُ `users`. ولا تُقاس المنحةُ بنجاح العبارة بل بـ
  `has_table_privilege`.
- **`WITH CHECK` الغائب يُستنسخ من `USING`.** فسياسةٌ شرطُها
  `user_id = auth.uid()` تحرس الصفَّ الجديد أيضاً بلا ثغرة. الخللُ في
  شكلين: أذرعٌ بـ`OR` (يُبقي المهاجم ذراعه ويُبدّل عمودَ الطرف الآخر)،
  وشرطٌ لا يذكر عمودَ الملكيّة أصلاً. والأوّل لا يُغلق إلّا بمُشغِّل، إذ
  `WITH CHECK` لا يرى `OLD` (0038، 0039).

## 🧪 أعراف العمل في هذا المستودع

- **كلّ إصلاحٍ يُقرَن بحارسٍ ساكن** في `tests/`، **ويُثبَت الحارسُ بكسر ما
  يحرسه** ثمّ إرجاعه. حارسٌ لم يسقط قطّ لم يُثبِت شيئاً.
  (وقعتُ في هذا مرّتين: مطابقةُ نصٍّ جزئيّ مرّت رغم إعادة تسمية الدالّة،
  وتعديلُ ملفٍّ لم يُكتب أصلاً.)
- **نجاح عبارة SQL ليس دليلاً.** `REVOKE` لمنحةٍ لم تُعطَ يمرّ صامتاً،
  و`UPDATE` لا يطابق صفوفاً تحت RLS ينجح بلا خطأ. القياس بـ
  `has_table_privilege` أو بعدد الصفوف المتأثّرة.
- **كلّ كتابةٍ على الإنتاج تُجرَّب أوّلاً** في `BEGIN … ROLLBACK`.
- الاختبارات: `npx jest` — يجب أن تمرّ كلّها قبل الدفع.

## ⏳ الرفض التلقائيّ (0042) — ما يجب معرفته قبل المسّ به

- **المدّة في `app_settings`** بالمفتاح `pending_auto_reject_hours`، لا في
  الكود. وغيابُ الإعداد أو صفرُه **يوقف** الرفض ولا يُرجعه إلى قيمةٍ
  مفترَضة — إلغاءُ طلبٍ طبّيّ لا يقع بقيمةٍ لم يخترها أحد.
- **`pg_cron` غير مثبَّت** في المشروع. المنفِّذ `/api/cron/auto-reject-pending`
  على جدولة Vercel، محميٌّ بـ`CRON_SECRET`. والمنطقُ كلُّه في SQL كي لا
  يتفرّق على موضعين إن أُضيف `pg_cron` لاحقاً.
- **الإشعار في المعاملة نفسها** (CTE واحد): إمّا أن يُلغى الطلبُ ويُخطَر
  المريض معاً، أو لا شيء.
- الحارس: `tests/auto-reject-stale.test.ts` — يفحص جسمَ الدالّة لا ترويسةَ
  تعليقاتها.

## 📨 طابور الإشعارات — ما يُسلَّم وما لا يُسلَّم

- `notification_queue` يستهلكه **معالجٌ واحد** (`notifications-processor.ts`)
  عبر كرون `/api/notifications/process`. ومساراتُ الدفع في `api/push/*` لا
  تقرأ الطابور أصلاً.
- القنوات المُنفَّذة: `whatsapp` و`push`. و`sms` **غير مُنفَّذة** وتفشل
  بوضوح. فلا تُدرج صفّاً بقناةٍ غير مُنفَّذة ظنّاً أنّه سيصل.
- القناةُ في الصفّ يجب أن تطابق `notification_templates.channel` للمفتاح
  نفسه — وإلّا وصل النصُّ في غير موضعه.
- `cancelled` تعني «لن يُسلَّم ولا عطبَ هنا» (مستخدمٌ بلا اشتراك دفع)، لا
  إخفاقاً. لا تُحوّلها إلى `failed`.
- الحارس: `tests/notification-push-channel.test.ts` — اختبارُ سلوكٍ لا
  مطابقةَ نصّ.

## 🔎 الفهرسة — ما قِيس في بناءٍ حقيقيّ

- **لا `alternates` في الجذر.** Next يُورّث metadata، فكان `canonical` الجذر
  يجعل كلَّ صفحةٍ عامّة تُعلن الرئيسيةَ أصلاً لها. كلُّ صفحةٍ تُعلن نفسها.
- **البيانات المنظَّمة بـ`<JsonLd>` لا بـ`next/script`.** ذاك يحقنها بالجافاسكربت
  فلا تراها زواحفُ الذكاء الاصطناعي. وFAQPage في الصفحة التي تعرض أسئلتها
  وحدها، مُشتقّةً منها.
- **التغطية في `src/lib/seo/coverage.ts` وحده** — لا تكتب محافظاتٍ في موضعٍ آخر.
- **لا `aggregateRating`** ما لم تكن مراجعاتٌ حقيقيّةٌ مرئيّة.
- `tsc` و`jest` لا يعرفان قاعدة `'use server'` — **شغّل `next build`** قبل
  الدفع حين تمسّ ملفّات الإجراءات (بمتغيّراتٍ نائبة تُرضي `env.ts`).
- الحارسان: `tests/seo-canonical.test.ts` و`tests/use-server-exports.test.ts`.

## 🪟 النوافذ وصفحة الطلب (الهاتف)

- **كلُّ نافذةٍ منبثقة جديدة** تُبنى على `ModalShell` (`components/ui/ModalShell.tsx`)
  أو `BottomSheet`، أو تستدعي `useModalDialog` مع `role="dialog"` و`aria-modal`.
  الخطّاف يجمع Escape وقفلَ تمرير الصفحة ونقلَ التركيز إلى النافذة — لا إلى أوّل
  حقل، كي لا تنبثق لوحةُ المفاتيح. الارتفاع بـ`dvh` لا `vh`.
- **صفحة `/appointments/new` بلا شريط تنقّلٍ سفليّ** (`FOCUSED_TASK_ROUTES` في
  `AppShell`). أشرطةُ الإرسال الثابتة تستقرّ على `bottom: 0` بمنطقة الأمان؛ لا
  تُرجعها إلى `64px`.
- **الشريط الثابت سطرٌ واحد**: التفاصيلُ داخل الصفحة، والحقولُ الناقصة بـ
  `<MissingFieldsSummary compact>`.
- **أنماطُ `styled-jsx` لا تحمي من الأصناف العامّة**: `.service-card` في
  `shared.css` قلبت بطاقاتِ المعالج عموداً لأنّ النمطَ المحلّيّ لم يذكر
  `flex-direction`. أصنافُ المعالج بالبادئة `aw-`.
- **لا رمزَ تحقّق (OTP) في رفع الطلب** — قرار المالك. `otp_verified` في
  `createAppointmentV2` قيمةٌ يرسلها العميل أصلاً، فالخطوة لم تكن حمايةً.
- **لا نافذةَ تنبثق في المهامّ المُركَّزة ولا في الطوارئ**
  (`src/lib/focused-routes.ts`): طلبُ الإشعارات وطلبا التثبيت كانت تغطّي
  «تأكيد الحجز» و«التالي».
- الحارس: `tests/order-flow-modals.test.tsx`.

## 🩺 شاشات المختص

- قِيست كلُّها بجلسة مختصٍّ محاكاة (360×640): لا فيض، ولا حقلَ يُكبّر iOS، ولا
  نصَّ دون 12px، ولا زرَّ أيقونةٍ بلا اسم. أرقامُ «إحصائيات اليوم» بـ
  `.spec-stat-value` لا بـ`.service-title` المستعار.
- الحارس: `tests/specialist-screens.test.ts`.

## 🦵 العلاج الطبيعي — «الحجز قريباً»

- `bookingSoon: true` في `services-v3.ts`: الصفحات مفتوحةٌ للتصفّح، وأزرارُ
  الحجز «الحجز قريباً»، و`/appointments/new?service=physio` تعرض شاشة «قريباً».
  يختلف عن شارة `badge: 'قريباً'` التي تمنع الدخول أصلاً.
- للتشغيل: احذف `bookingSoon` وأضف خدمةَ العلاج الطبيعي (سعر، مدّة) إلى المعالج.
- الحارس: `tests/physio-booking-soon.test.tsx`.

## 📞 أرقام المنشآت

- ١٠٢ رقماً مُختلَقاً في الإنتاج قُنّعت في 0045 («0770 xxx xxxx»)، وكذلك البذور
  وقوائم الزائر. `122` حقيقيٌّ وباقٍ. رقمُ الشركة 07803993585 حقيقيٌّ ولم يُمسّ.
- كلُّ رابط `tel:`/`wa.me` لرقم منشأةٍ يمرّ بـ`<PhoneLink>`: الرقمُ المُقنَّع زرٌّ
  معطّل لا رابط. أدخِل الرقمَ الموثَّق من لوحة الإدارة فيعود الزرّ تلقائياً.
- الحارس: `tests/facility-phones.test.tsx`.

## 🔀 سياسات RLS مدمجة (0046) — بموافقة المالك

- سياسةٌ واحدة لكلّ (جدول، أمر) على 44 جدولاً: USING = OR، وWITH CHECK = OR على
  `coalesce(WITH CHECK, USING)` — مكافئٌ حرفيّاً لسياساتٍ متساهلة متعدّدة.
  قِيس: ٥٢٨ فحصاً (4 هويّات × قراءة/تحديث/حذف) على 6,974 صفّاً — صفرُ فرق.
  والمدقّق 323 → 8 (كلّها `lab_results` المستثنى لسياساته المرتبطة بأدوار).
- **سياسةٌ جديدة لجدولٍ مدمج**: عدّل السياسةَ `<table>_<cmd>_merged` بإضافة ذراع
  `OR`، ولا تُضف سياسةً ثانيةً للأمر نفسه فيعود التراكب.
- `bug_reports` بلا سياسة إدراج (كان `WITH CHECK (true)` لأيّ زائر).
- الحارس: `tests/policy-merge.test.ts`.

## 🔢 أرقامُ التطبيق حيّة

- تقييماتُ المختبرات كانت مكتوبة («4.9 · 1240+ تقييم» في `labs-data.ts`، و4,230
  مراجعةً مبذورة في `partner_labs`) ولا جدولَ تقييماتٍ للمختبرات. أُزيلت من
  الواجهة وصُفّرت في 0047. تعود حين يوجد مصدرٌ يُحسب منه.
- الحارس: `tests/app-live-numbers.test.ts`.

## 🔑 معلّقٌ على المالك وحده

(تحقّقتُ بعد نشر 8719bd1: لا أخطاءَ تشغيل في Vercel خلال ٧ أيّام، وصفحاتُ التسويق
تُخدَم 200 بـcanonical ذاتيّ وJSON-LD من الخادم و`index, follow` — فـ`SITE_TYPE`
ليست `app`.)

- تفعيل حماية كلمات المرور المسرَّبة في لوحة Supabase (إنذار المدقّق الأخير).
- **159 فهرساً «غير مستعمَل»** لا تُحذف: القاعدة بلا حركة (٨ مستخدمين).
- أرقامُ المنشآت مُقنَّعة (انظر 📞) — إدخالُ الأرقام الموثَّقة من لوحة الإدارة.
- **أرقامُ الصفحة الرئيسية** («+5000 مستخدم»… في `Stats.tsx` و`ACTIVE_CITIES`)
  **تبقى — قرار المالك: للتسويق فقط.** أمّا داخل التطبيق فأرقامٌ حيّة وحدها
  (انظر 🔢).
- **قائمة `SERVED_CITIES` تحتاج تأكيداً** — بُذرت من خريطة الرئيسية.
- التحقّق في Google Search Console (الرمز معلَّقٌ في `layout.tsx`) ورفعُ الخريطة.
