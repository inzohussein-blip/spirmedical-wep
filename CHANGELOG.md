# Changelog

## [0.7.0] - 2026-07-25

تصليب الأنواع (إزالة الـ casts العريضة) + كشف وإصلاح ٣ أخطاء إنتاجية صامتة عبر تدقيق
كل عمليات الكتابة والقراءة في الجداول خارج نطاق فحص TypeScript مقابل الـ DDL الفعلي.

### Fixed — أخطاء إنتاجية صامتة (أعمدة خاطئة + خطأ مُبتلَع)
- **الحملات لا تصل أحداً:** `sendCampaign` كان يُدرج في `notification_queue` أعمدة غير
  موجودة (`user_id`/`notification_type`/`payload`) ويُغفل أعمدتها الإلزامية
  (`recipient_phone`/`channel`/`body`)، فكل إدراج يفشل بصمت (`if (!error)`) والحملة
  تُوسَم «مُرسَلة» بلا وصول. صُحِّح ليطابق DDL الترحيل والـ helper المرجعي؛ حملات البريد
  تُرفض صراحةً (القناة تقبل whatsapp/sms/push فقط) ومن لا هاتف له يُتجاوَز.
- **تقييمات الأسنان/البصريات لا تُحفظ:** `submitRating` كان يكتب في `dental_ratings`
  و`optical_ratings` بأسماء أعمدة غير موجودة (`cleanliness_rating`/`skill_rating` بدل
  `hygiene_rating`/`expertise_rating`؛ `product_quality_rating` بدل `quality_rating`)،
  والخطأ مُهمَل. صُحّحت الأسماء لتطابق `0005_specialists_ratings.sql`. (جدول `ratings`
  العام كان يُكتب بنجاح دائماً، فلم تُفقَد التقييمات كلياً — فقط النسخ المتخصّصة.)

- **الأدمن لا يستطيع إلغاء أي طلب:** `adminCancelOrder` كان يكتب في
  `appointments.cancellation_reason` وهو عمود غير موجود (اسمه `cancelled_reason`؛
  `cancellation_reason` يخصّ `pharmacy_reservations`). الخطأ كان مفحوصاً هنا فلم يكن
  صامتاً — لكنّ الإلغاء كان **يفشل كلياً**. وكان نوع `appointments` المكتوب يدوياً يحمل
  الاسم الخاطئ نفسه، فامتدّ الخلل إلى القراءة: صفحة تفاصيل الموعد كانت تقرأ العمود
  الخاطئ فلا يرى المريض سبب الإلغاء أبداً. صُحّح الثلاثة (النوع + الكتابة + القراءة).

- **ثلاث طبقات غائبة عن خريطة الخدمات:** `services/page.tsx` كان يستعلم
  `mental_health_specialists.city` و`nutritionists.city` (الاسم الصحيح `clinic_city`)،
  ويجلب الأطباء من `users` مستعملاً `latitude`/`longitude`/`is_active` — وهي أعمدة غير
  موجودة في `users` إطلاقاً. والصفحة **تتجاهل الأخطاء صراحةً** (`// تجاهل أي errors`)،
  فكانت ثلاث طبقات من سبع تختفي من الخريطة بصمت. صُحّحت بأسماء مستعارة (`city:clinic_city`)
  ونُقل مصدر الأطباء إلى جدول `doctors` (إحداثياته `clinic_lat`/`clinic_lng`).
- **إحصاءات المختصّ لا تعرض «أكثر الخدمات»:** كانت تستعلم `appointments.service_name`
  وهو غير موجود (الصحيح `service_type`)، فيعود الاستعلام فارغاً دائماً — رغم أنّ التعليق
  يَعِد بـ«عدّ فعلي لا أرقام ملفّقة».

### Added — حارس تطابق المخطّط (يمنع تكرار الفئة)
- `tests/schema-conformance.test.ts`: يقرأ الـ DDL الحقيقي من الترحيلات (بما فيه
  `ALTER TABLE … ADD COLUMN` متعدّد الأعمدة) ويقارنه بأعمدة كل `insert`/`update`/`upsert`
  في المصدر، فيفشل البناء عند أيّ عمود غير موجود — سواء أكان الجدول مُعرَّفاً في الأنواع
  أم لا، وسواء عُطِّل الفحص بـ cast أم لا. يتخطّى بأمان ما لا يُحلَّل ساكناً (spread /
  مفاتيح محسوبة). يتضمّن اختبارات للحارس نفسه تُثبت كشفه للأخطاء التاريخية بلا إنذار كاذب.
  **هذا الحارس هو ما كشف خلل الإلغاء أعلاه** بعد أن فات التدقيق اليدوي.
- تغطية الحارس النهائية: أعمدة الكتابة · أعمدة `select` (مع الأسماء المستعارة والعلاقات
  المُضمَّنة) · أعمدة الفلترة/الترتيب (`eq`/`not`/`order`/…) · **الفلترة المركّبة `or`** ·
  **الاستعلامات المنفصلة عن `from`** (نمط `q = q.or(…)` بتتبّع المتغيّر → الجدول) ·
  **أسماء الجداول نفسها** (تمسك الأخطاء المطبعية؛ صفر مخالفات حالياً).

### Fixed — رفع الطلبات ووصولها (تدقيق دورة الحياة كاملةً)
تُتُبِّعت دورة الطلب من الإنشاء إلى العرض: **7 مسارات إنشاء** مقابل **25 واجهة عرض**.
- **طلبات المعالج العامّ لم تكن تصل أيّ مختصّ.** طابور المختصّ يفلتر بصرامة على
  `required_specialist_type`، و`createAppointmentV2` لم يكن يضبطه إطلاقاً — فكل خدمة
  بلا تدفّق مخصّص (تركيب مغذي · فحص PCR · الاستشارات الثلاث · توصيل الأدوية · طبيب
  الأسرة) تُنشئ طلباً يراه المريض والأدمن **ولا يصل لمن ينفّذه**، فيبقى «قيد الانتظار»
  أبداً. صار الكتالوج مصدراً موحّداً (`Service.specialistType`) ويشتقّ المسارُ العمودَ
  منه. حجز المستشفى مُستثنى عمداً (منشأة ينسّقها الأدمن) والاستثناء صريح ومُسجَّل.
- **التحقّق:** الأدمن يرى كل الطلبات (بلا فلتر أساسي) · المريض يرى طلباته (`user_id`)
  · المختصّ يرى نوعه — المصفوفة متّسقة الآن.

### Fixed — الخرائط لا تُسقط رفع الطلب عند ضعف الشبكة
كان `loadMapLibre()` يُستدعى **بلا `try/catch`** ولا استماعَ لحدث `error`. فلو فشل
تحميل المكتبة أو البلاط (شبكة ضعيفة/حجب) يبقى المربّع **رمادياً صامتاً للأبد** — وفي
منتقي الموقع أثناء الحجز يعني ذلك أنّ المريض لا يفهم لماذا لا يستطيع تحديد عنوانه، فقد
يترك الطلب.
- `UserLocationPicker`: حالات صريحة (تحميل / جاهزة / فشل)، ورسالة الفشل **توجّه إلى
  حقل العنوان اليدوي** الموجود أصلاً — والتحقّق لا يشترط GPS، فالطلب يكتمل بلا خريطة.
- `SpirMapView` (خرائط الخدمات والأدمن): كان **بلا أي معالجة أخطاء**؛ صار يُظهر
  «تعذّر تحميل الخريطة» بدل مربّع فارغ.

### Fixed — لا تأكيد بعد رفع الطلب (`?new=1` مُهمَل)
تُوجّه تدفّقات الحجز إلى `/appointments/[id]?new=1` بعد النجاح، لكنّ المُعامل **لم يكن
يُقرأ في أي مكان** — فيصل المريض في أهمّ لحظة (بعد إرسال طلبه) إلى صفحة عادية بلا أيّ
تأكيد أنّ الطلب وصل. أُضيف تأكيد صريح يشرح الخطوة التالية (إبلاغ المختصّين ثمّ إشعار عند
القبول). ووُحِّدت وجهة المعالج العامّ مع التدفّقين الآخرين (كان يذهب للقائمة لا للطلب).

### Added — حماية الطلبات من الإرسال المزدوج (وُصِّلت `idempotency`)
كانت الحماية مكتوبة وغير موصولة. الآن تلتفّ حول مسارات الإنشاء الثلاثة (سحب الدم ·
التمريض · المعالج العامّ): ضغطتان سريعتان على «تأكيد» — أو إعادة إرسال بعد انقطاع
شبكة — تُنتجان نفس المفتاح فتُنفَّذ العملية **مرّة واحدة** وتُعاد نتيجتها.
- **الخطر الذي عولج أولاً:** الحارس الأصلي كان يخزّن **أي** نتيجة. لو خُزّن الفشل
  لبقي المريض عالقاً 24 ساعة يتلقّى نفس رسالة الخطأ حتى بعد تصحيح بياناته. الآن
  **لا تُخزَّن إلا النتائج الناجحة** (يدعم اصطلاحَي `success` و`ok`).
- **المفتاح مبنيّ ليميّز الطلبات المشروعة:** الخدمة + المستخدم + الموعد + العنوان +
  **فرد العائلة** + (التحاليل/الإجراء) — فطلبان لفردين مختلفين بنفس التوقيت لا يتداخلان.
- **فشل طبقة الحماية لا يمنع الطلب**: تعطُّلها يُسجَّل ويُمرَّر، فلا تمنع مريضاً من
  رفع طلب.
- تنظيف المفاتيح المنتهية أُضيف إلى cron التذكيرات اليومي (كانت `cleanupExpiredKeys`
  معرّفة وغير مستدعاة، فكانت المفاتيح ستتراكم).

### Removed — كود ميت (محذوف بعد تحقّق آلي)
مسحٌ للتصديرات والملفات: **14 ملفاً لا يستورده أحد**. حُذف منها ما هو **بقايا إعادة
هيكلة** فقط (~1574 سطراً)، بعد التأكّد من صفر مراجع (بما فيها الاستيراد الديناميكي):
- `login/LoginClient.tsx` (530 سطراً) — صفحة الدخول تبني واجهتها بنفسها الآن؛ وجود
  نسخة كاملة ميتة بجانبها فخٌّ لأي تعديل مستقبلي.
- `dashboard/search-client.tsx` · `dashboard/pills-client.tsx` — بقايا إعادة بناء اللوحة.
- `dashboard-v3/ServiceDetailHero.tsx` · `landing/LandingInstallButton.tsx` ·
  `ui/BottomDrawer.tsx` · `pwa/SwipeGestures.tsx` — مكوّنات غير مستهلَكة.
- `auth/email-auth.ts → signInWithGoogle` — تجاوزه تدفّق OAuth عبر `/auth/callback`.

**لم تُحذف عمداً** (بنية لميزات غير مبنيّة بعد، لا بقايا): `lib/bluetooth.ts` (أجهزة
طبّية) · `lib/i18n/` · `lib/permissions/` · `lib/icons.ts` · `lib/idempotency.ts`
(حماية من الإرسال المزدوج — جاهزة وغير موصولة؛ وصلها قرار منتج لأنّ الخطأ في المفتاح
قد يبتلع طلباً مشروعاً). قرار الإبقاء/الحذف للمالك.

### Verified — لا مسارات ناقصة
- كل رابط داخلي و`fetch('/api/…')` يصل مساراً موجوداً (169 صفحة · 26 معالجاً) — يحرسه
  `tests/route-links.test.ts`.
- **كل رابط عميق داخل الإشعارات** (`/appointments/[id]` · `/specialist/orders/[id]` ·
  `/messages/[id]` · `/consultations/[id]` · `/appointments/[id]/track`) يصل صفحةً
  موجودة — فلا إشعار يقود إلى 404.
- رسائل «قريباً/قيد التطوير» المتبقّية (الدفع الإلكتروني · الحجز المباشر مع المستشفيات ·
  الدفع بالرصيد) **إفصاحات منتج صحيحة**، لا ميزات مكسورة.

### Added — إشعار المختصّين بالطلبات الجديدة
بعد أن صارت الطلبات تصل الطابور، بقي سؤال: **هل يعلم بها أحد؟** قالب
`notifyNewOrderForSpecialist` كان موجوداً لكنّه يُستدعى **فقط عند إسناد الأدمن
يدوياً** — فالطلب الذي يرفعه المريض ينتظر صامتاً حتى يفتح مختصٌّ التطبيق صدفةً.
- `notifyEligibleSpecialistsOfNewOrder` جديدة: تُشعر كل مختصّ **معتمَد** من النوع
  المطلوب (`role='specialist'` + `approval_status='approved'`) عبر عميل خدمة (المريض
  لا يملك صلاحية قراءة قائمة المختصّين)، وتحلّ اسم المريض بنفسها فلا يتغيّر أي موضع نداء.
- وُصِّلت بمسارات الإنشاء الأربعة: المعالج العامّ · سحب الدم · التمريض · حجز الطبيب
  (**هذا الأخير لم يكن يُشعر أحداً إطلاقاً — لا المريض ولا المختصّ**).
- إن لم يوجد مختصّ معتمَد لهذا النوع يُسجَّل تحذير صريح — إشارة تشغيلية للمالك بدل صمت.
- fail-safe بالكامل: الإشعار مكمّل، لا يُفشل الطلب أبداً.

### Added — التفاصيل السريرية في صفحات الطلب (المختصّ · الأدمن · المريض)
تلتقط تدفّقات الطلب بياناتٍ منظّمة حرجة لكنّها **لم تكن تُعرض لأحد**: كان المختصّ يرى
التاريخ والعنوان والملاحظات فقط، فيصل الممرّض إلى المنزل **دون معرفة حساسية المريض ولا
احتياطات العدوى**. مكوّن `OrderClinicalDetails` الجديد يعرضها مرتّبةً بالأولوية السريرية:
تنبيه العدوى (أحمر) ثم الحساسية (كهرماني) ثم الصيام · بيانات الفحص · تفضيل الكادر ·
صورة الوصفة · المستلزمات · الزيارة المتكرّرة · موقع GPS (زرّ خرائط) · المدة والمبلغ.
حين تكون الاستمارة مملوءة بلا حساسية يُعرض تأكيد صريح بدل الفراغ الملتبس، ولا يظهر
القسم إطلاقاً إن لم توجد بيانات.

### Performance — توازي الاستعلامات المستقلّة
مسحٌ للاستعلامات المتسلسلة وجد أنّ الأنماط الحرجة سليمة أصلاً (`push` وتذكيرات
المواعيد تستخدمان `Promise.all`، وcron التذكيرات يتجنّب N+1 بجلب المستخدمين دفعةً
واحدة عبر `.in()`). عولجت ثلاثة مواضع تُنفَّذ عند تحميل الصفحة:
- **مواقع الأدمن** (`getAllLocations`): كانت 7 رحلات متتالية للقاعدة (جدول لكل مصدر)
  → صارت متوازية.
- **إحصاءات المختصّ**: 4 استعلامات مستقلّة (إجمالي/مكتمل/ملغى/أكثر الخدمات) → متوازية.
- **المفضّلة**: 5 استعلامات تفاصيل مستقلّة → متوازية عبر جدول تكوين (أزال أيضاً خمس
  كتل مكرّرة)، ولا يُستعلَم إلا عن الأنواع الموجودة فعلاً.

### Added — حارس الروابط والمسارات
- `tests/route-links.test.ts`: يبني خريطة مسارات App Router من الملفات (مُدركاً
  مجموعات المسارات `(group)` والمقاطع `[id]`/`[...slug]`) ويتحقّق أنّ كل رابط داخلي
  (`href`/`router.push`/`redirect`) وكل `fetch('/api/…')` يقابله مسار موجود.
  **النتيجة الحالية: صفر روابط ميتة** عبر 169 صفحة و26 معالج مسار — أي أنّ إصلاح
  الروابط السبعة في 0.5.0 صامد. الحارس يمنع عودتها، ولمُطابِقه اختباراته الخاصّة
  التي تُثبت أنّه يرفض المسارات غير الموجودة (لا يمرّ صامتاً).

### Changed — إظهار الأخطاء المُبتلَعة في المسارات الطبّية الحرجة
الفشل الصامت هو ما أخفى الأخطاء أعلاه: مسح الكود وجد **81** عملية كتابة تُهمَل نتيجتها.
معظمها «أطلق وانسَ» مشروع (تحليلات/سجلّات تدقيق)، فلم تُغيَّر جملةً واحدة؛ عولجت
المسارات التي يعني فشلها الصامت فقدان بيانات طبّية أو حالة خاطئة يراها المريض:
- **حفظ نتائج التحاليل** (`saveLabResults`): فشل حذف النتائج السابقة كان يُنتج نتائج
  مُكرَّرة للمريض → صار يتوقّف بخطأ صريح. وفشل تحديث حالة `lab_orders`/`appointments`
  كان يعني أنّ المريض **لا يرى «النتائج جاهزة» أبداً** رغم حفظها → صار يُسجَّل.
- **التقييمات المتخصّصة**: فشل إدراجها صار يُسجَّل بدل ابتلاعه (وهو الصمت الذي أخفى
  خلل أسماء الأعمدة أصلاً).
- **إنشاء طلب سحب الدم**: فشل التراجع (يترك `lab_order` يتيماً) وفشل ربط الموعد بالطلب
  (فلا يظهر في سجلّ التحاليل) صارا يُسجَّلان.

### Changed — تصليب الأنواع (٧ دفعات)
- إزالة الـ casts العريضة (`supabase as any` / شيم `as unknown as { from }`) من ~20 ملفاً
  بعد تعريف جداولها في `types/database.ts`، بالاعتماد على العميل المُعرَّف. تُركت casts
  مُضيَّقة مبرَّرة فقط: joins عميقة، أسماء جداول ديناميكية، وجداول ما زالت خارج الأنواع
  (تُحسم آلياً بـ `db:types` على المخطّط الحيّ). لا تغيير سلوكي — الاستعلامات ذاتها.

### Audited — سلامة طبقة قاعدة البيانات
- دُقّقت كل عمليات `insert`/`update`/`upsert`/`select` على الجداول غير المُعرَّفة
  والديناميكية مقابل الـ DDL: البقية سليمة (`hospital_ratings`، `cosmetic_*`،
  `pharmacy_*`، `service_favorites`، `admin_requests`، مواقع الأدمن، اللقاحات، idempotency).

## [0.6.0] - 2026-07-22

إعادة بناء الخرائط · توحيد «رفع الطلبات» بخاصية الحقول الناقصة · دخول التطبيق لا التسويق
· أتمتة ترحيلات Supabase. (لا تغيير على بيانات الإنتاج.)

### Changed — الخرائط (إعادة بناء)
- **الانتقال من Leaflet + OpenStreetMap (raster) إلى MapLibre GL + OpenFreeMap (vector):**
  المزوّد العام كان يمنع الاستخدام التجاري (418/403) فتظهر الخريطة مكسورة. المزوّد الجديد
  مجاني بلا مفتاح API ويسمح بالتجاري. أُعيد بناء المكوّنات الستّة (عرض/منتقيات/حرارة/تغطية)
  مع تجميع (clustering) أصلي في خريطة الخدمات.
- **إصلاح العربية على الخريطة (RTL):** تسجيل `setRTLTextPlugin` (ملف مُستضاف ذاتياً) فأصبحت
  التسميات العربية موصولة وباتجاه صحيح بدل المعكوسة.

### Added — «الحقول الناقصة» (توحيد رفع الطلبات)
- أدوات مشتركة محايدة التنسيق: `useFormErrors` + `MissingFieldsSummary` + `FieldError`.
- تُخبر المريض صراحةً بالحقول غير المملوءة (أخطاء مضمّنة + صندوق قابل للنقر + تمرير/تركيز
  لأوّل خطأ + مسح عند التعديل)، بدل الزرّ المُعطَّل الصامت والتوست المفرد. طُبِّقت على 11
  تدفّقاً/نموذجاً: سحب الدم/التحاليل (إعادة بناء كامل + منتقي خريطة)، التمريض المنزلي،
  الخدمات العامة (Wizard)، الصيدلية، الوصفة، التذكير، العائلة، الشكاوى، بلاغ العطل،
  المواقع المحفوظة، تعديل الملف، الأدوية، التقييم.

### Changed — الدخول والتوجيه
- **الدخول يقود دائماً إلى واجهة التطبيق لا التسويق:** أي مستخدم مُسجّل يفتح `/` يُوجَّه
  مباشرة إلى التطبيق (كان يرى التسويق في المتصفّح العادي).
- توحيد منطق «الدور → مسار البيت» في مصدر واحد `lib/auth/home-path.ts` (كان مكرّراً في 8
  ملفات بصيَغ متعارضة). تثبيت PWA = التطبيق فقط (start_url=/app + StandaloneAppGuard).

### Added — أتمتة Supabase migrations
- `supabase/config.toml` + workflow `supabase-migrations.yml` (يطبّق `db push` عند الدفع
  إلى main) + سكربتات `db:*` — تطبيق الترحيلات تلقائياً بلا رفع يدوي (يتطلب سرّين من المالك).

## [0.5.0] - 2026-07-09

بناء "النسخة الكاملة": إصلاح التدفقات المكسورة + إحياء طبقة التوصيل + تنظيف بصري آمن
+ تصليب. (لا تغيير على بيانات الإنتاج.)

### Fixed — تدفّقات المستخدم (المرحلة 1)
- **بحث حقيقي:** صفحة `/search` جديدة (كان البحث الرئيسي يقود إلى 404) تبحث في
  كتالوج الخدمات/الأدوات/الفحوصات مع تطبيع عربي ونتائج مجمّعة.
- **تتبّع طلب حقيقي:** `/appointments/[id]/track` يقرأ حالة الموعد من DB + realtime
  بدل محاكاة setTimeout وسعر مزيّف.
- **مرفقات المحادثة:** رفع صور فعلي (bucket خاص + رابط موقّع محميّ بعضوية المحادثة)؛
  حُذفت خيارات "قيد التطوير" الوهمية.
- **روابط ميتة:** تصويب 6 روابط + إنشاء صفحة `/admin/emergencies/[id]` (وجهة تنبيه
  الطوارئ التي كانت 404).
- **أزرار ميتة (الضيف):** إزالة زر صوت وهمي، وربط أزرار الستوريز بطلب التسجيل.

### Fixed — طبقة التوصيل (المرحلة 2)
- **الإشعارات تعمل فعلاً:** كان الكرون يستدعي GET (فحص صحة) والمعالج يُحجب بـ RLS؛
  الآن معالج بصلاحية الخدمة يعمل على GET+POST، الحدّ 20→100، وإرسال فوري inline
  لإشعارات المعاملات.
- **الكرونات fail-closed:** بدون `CRON_SECRET` تُرفض (كانت عامّة).
- **تذكيرات:** كرون `/api/cron/reminders` يقرأ جدول `reminders` ويُرسل (كان لا مُرسِل).
- **تقييمات الممرضين:** تُقرأ من جدول `ratings` الفعلي (كانت اللوحة فارغة دائماً).
- **حذف البيانات:** يُحفظ في سجلّ التدقيق (كان يُرسل بريداً فقط).
- إزالة stubs مزامنة SW الكاذبة، وإيقاف تخزين صور base64 داخل صفوف DB.
- توثيق الأسرار في `env.ts`: `CRON_SECRET`، `META_APP_SECRET`، `VAPID_*`، `TWILIO_*`.

### Changed — تنظيف بصري آمن (المرحلة 3، جزئي)
- حسم تعارضات CSS المكرّرة (`.app-shell`/`.app-bottom-nav`/`.scr-header-v2`) ودمج
  keyframes المكرّرة — بلا تغيير بصري (أُبقيت النسخة الفائزة في الـcascade).
- (مؤجَّل: استبدال الألوان بالتوكنز وتوحيد المكوّنات — يحتاج QA بصرياً على preview.)

### Security — تصليب (المرحلة 4)
- تحقّق Zod على `api/whatsapp/otp/send|verify` (رقم عراقي + قناة/رمز) + rate limit
  على verify (دفاع ضد التخمين).

## [0.4.2] - 2026-07-05

مراجعة أمان وتصحيح واسعة (لا تُغيّر بيانات الإنتاج).

### Security
- **التشفير:** `ENCRYPTION_KEY` صار مطلوباً دائماً؛ أُزيل المفتاح الاحتياطي المعروف
  (`sha256('dev-key-only-not-secure')`) الذي كان يشفّر PHI خارج الإنتاج.
- **باب OTP خلفي:** حُذف قبول الرمز `123456` (في `otp-channels.ts` و`verifyOtpAction`)؛
  تدفّق الحجز يستخدم الآن خدمة OTP الحقيقية (bcrypt + DB).
- **الدخول بدون رمز:** يُقفَل في وضع `NEXT_PUBLIC_OTP_MODE=required` (الافتراضي الحالي
  `optional` مؤقتاً لإبقاء الدخول شغّالاً حتى تفعيل قناة OTP).
- **فصل الأسرار:** كلمة سر حسابات الهاتف تُشتَقّ من `AUTH_PASSWORD_SECRET` منفصل عن
  `ENCRYPTION_KEY` (توافق رجعي + تدوير تلقائي عند أول دخول).
- **الأدمن:** إغلاق تصعيد الصلاحيات عبر النموذج (rate limit + مفتاح قوي/جلسة super_admin +
  سقف الدور + allowlist)، وحماية `/admin` على مستوى middleware بفحص الدور.
- **Rate limiting:** أُضيف لـ otp/send، admin-login/register، nurse/emergency،
  analytics/track؛ وأُصلح fail-open في فحص حدّ OTP.
- **السجلّات:** حجب `phone`/`email` (PHI) ووقف تسجيل قيم OTP.
- **التحقّق:** مخططات Zod على `nurse/emergency` و`analytics/track`.

### Fixed
- **الخرائط:** كانت مكسورة لأن CSS الخاص بـ Leaflet/MarkerCluster يُحمَّل من unpkg بينما
  CSP يحجبه؛ يُجمَّع الآن محلياً من node_modules.
- **روابط مكسورة (404):** تصحيح 31 موضعاً كان يشير لـ `/auth/login|register|forgot`.
- **ترتيب migrations:** كان email-auth يُمحى بالـ DROP قبل إعادة بنائه؛ صار يعمل بعد الأساس.
- تأكيد البريد يُرسَل فعلاً عبر Resend (مع بوابة مشروطة بتوفّره)، وزر Google OAuth حقيقي،
  وإحصائيات الأخصائي حقيقية بدل أرقام ملفّقة.

### Changed
- إعادة تسمية لوحة الأدمن `admin44` → `admin` (خلف حماية middleware) مع تحويلات دائمة.
- توحيد مساعدات auth المكرّرة في `src/lib/auth/` (بيانات الاعتماد، getIp، isNextRedirect).
- تقسيم مخطط قاعدة البيانات إلى 10 وحدات ترحيل مرتّبة وغير مُدمِّرة (تحقّق byte-identical)،
  وعزل سكربت المسح في `supabase/reset/`.
- منع مزوّد WhatsApp الوهمي في الإنتاج؛ إخفاء قناة Telegram المعطّلة خلف علم.

### CI
- بوّابة الجودة (type-check + lint + test + build) تعمل الآن على دفع أي فرع.

## [0.4.0] - 2026-05-09

### Added
- AppShell موحّد بعرض هاتف ثابت (480px)
- Dark Mode كامل مع toggle و localStorage
- Favicons احترافية (٧ أحجام)
- A11y compliance (skip link, ARIA, keyboard nav)

### Changed
- توحيد layouts بين guest و dashboard
- استبدال نظام تسجيل الدخول بـ OTP فقط

### Removed
- مجلد `(admin)/` - منقول لمشروع CRM منفصل
- `BottomNav.tsx` القديم - مدمج في AppShell
- جميع حسابات الاختبار من `actions.ts`
- نظام `loginWithCredentials` (رقم/رمز)
- `TEST_MODE` و `TEST_ACCOUNTS`

## [0.3.0] - 2026-04
- Patient Appointments V2
- Supabase setup

## [0.2.0]
- Security hardening
- Audit logs

## [0.1.0]
- Initial release
